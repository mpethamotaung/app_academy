#List  allow us to work with sequential data
courses = ["History", "Math", "Physics", "CompSci"] # has length of 4, index 0 to 3 (length of list - 1)

print(courses) #prints entire list
print(len(courses)) #prints number of values in the list
print(courses[2]) #prints the value at index
