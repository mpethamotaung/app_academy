greeting ="Hello"
name = "Michael"

print(dir(name)) #shows all attributes and methods that we have access to with the variable
# help(): remembering what a method does or what you can do with a type 
print(help(str)) #runs on the string class itself, everything that is available
print(help(str.lower)) #tells us what the method does